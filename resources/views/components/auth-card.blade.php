{{-- <div class="min-h-screen flex items-center justify-center bg-gray-100 dark:bg-gray-900">
    <div class="w-full max-w-md p-6 bg-white dark:bg-gray-800 shadow-lg rounded-2xl">
        <header class="">
            <title>@yield("title","defualt title for auht pages") || {{ config("app.name",'Bebin')}}</title>
        </header>
        App Logo or Title
        <div class="flex justify-center mb-6">
            <a href="{{ url('/') }}" class="text-2xl font-bold text-red-600">
                Bebin
            </a>
        </div>

        Card Body (slot for dynamic content)
        <div>
            {{ $slot }}
        </div>
    </div>
</div> --}}
